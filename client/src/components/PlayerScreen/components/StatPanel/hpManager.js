import React, { Fragment, useState } from "react";
import { connect } from "mongoose";

const hpManager =() => {

    return(
        <Fragment>

        </Fragment>
    )
}

export default connect()(hpManager)